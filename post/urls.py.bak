from __future__ import absolute_import
from django.conf.urls import url
from .views import *

from django.contrib.auth.decorators import login_required


urlpatterns = [

    url(ur'^(?P<pk>\d+)/$', Post_page.as_view(), name=u"currentpost"),
    url(ur'^(?P<pk>\d+)/comments/$', Post_comments.as_view(), name=u"postcomments"),
    url(ur'^(?P<pk>\d+)/edit/$', login_required(EditPost.as_view()), name=u"editpost"),
    url(ur'^posts/(?P<pk>\d+)/like/$', login_required(PostLikeView.as_view()), name=u"postlikes"),

]

